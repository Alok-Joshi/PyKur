import websocket-client
from .media_object import media_object

class pipeline(media_object):
    def __init__(self):
        self.session_id = None
        self.object_id = None

     def create(endpoint_type): 
        """ Creates the endpoint_type element on the Kurento Media Server """
        pass;
